<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8" />
<title th:text="${titulo}">Insert title here</title>
<link rel="stylesheet"
	href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>

	

	<div class="container">
		<h1
			class="text-secondary border border-success border-top-0 border-left-0 border-right-0"
			th:text="${titulo}"></h1>
		<p><a th:href="" class="btn btn-success btn-xs">Registrar</a></p>
		<table class="table table-striped">
			<thead>
				<tr>
					<th>id</th>
					<th>Nombre</th>
					<th>Categoria</th>
					<th>Precio</th>
					<th>Descuento</th>
					
				</tr>
			</thead>
			<tbody>
				<tr th:each="Producto: ${producto}">
					<td th:text="${producto.id}"></td>
					<td th:text="${producto.Nombre}"></td>
					<td th:text="${producto.Categoria}"></td>
					<td th:text="${producto.Precio}"></td>
					<td th:text="${producto.Descuento}"></td>								
					<td><a class="btn btn-primary btn-xs" th:href="${producto.id}" th:text="'editar'"></a></td>
					<td><a class="btn btn-danger btn-xs" th:href="${producto.id}" th:text="'eliminar'" onclick="return confirm('Estás seguro que quieres eliminar?');"></a></td>
				</tr>
			</tbody>

		</table>
	</div>
	<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
	<script
		src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
	<script
		src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>